<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPDATE</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <h2>Ügyfél módosítása</h2>
    <form action="/edit/<%= ugyfel.id %>" method="post">
        <div class="mb-3">
            <label for="azon" class="form-label">Azonosító</label>
            <input type="number" min="2000" value="<%= ugyfel.azon %>" class="form-control" id="azon" name="azon" required>
        </div>
        <div class="mb-3">
            <label for="nev" class="form-label">Neve:</label>
            <input type="text" value="<%= ugyfel.nev %>" class="form-control" id="nev" name="nev" required>
        </div>
        <div class="mb-3">
            <label for="szulev" class="form-label">Született</label>
            <input type="number" min="1960" value="<%= ugyfel.szulev %>" class="form-control" id="szulev" name="szulev"
                required>
        </div>
        <div class="mb-3">
            <label for="irszam" class="form-label">Irszám:</label>
            <input type="number" value="<%= ugyfel.irszam %>" placeholder="1234" maxlength="4" class="form-control" id="irszam"
                name="irszam" required>
        </div>
        <div class="mb-3">
            <label for="orsz" class="form-label">Nemzetiség:</label>
            <input type="text" maxlength="4" value="<%= ugyfel.orsz %>" class="form-control" id="orsz" name="orsz" required>
        </div>
      <button type="submit" class="btn btn-primary">Mentés</button>
    </form>
  </div>

  <!-- ... -->
</body>
</html>
